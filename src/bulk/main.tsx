import '../popup/app/globals.css';
import React from 'react';
import ReactDOM from 'react-dom/client';
import BulkApp from './BulkApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BulkApp />
  </React.StrictMode>
);
